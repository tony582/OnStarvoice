# StarVoice 架构优化实施方案与计划

> 状态：P0、P1 与 G1 已关闭；P2-A1 已通过 PR #19 合并至 `main@0ec5508`，并以 4 个 Server 文件白名单完成限定生产发布；P2-A2 已通过 PR #20 合并至 `main@5b66004`；P2-B 已于 2026-08-15 通过 PR #21 合并至 `main@7c3e47ef1717a3fc498d63a1e3b7952e9100ecee`，但未部署；P2-C 实现提交 `bc4d665` 已推送并创建 Draft PR #22，尚未合并或部署；整体 G2 尚未关闭
> 制定日期：2026-08-12
> 审查基线：`codex/edit-unattended-plans`，`1332176`
> P1 实施分支：`codex/architecture-modernization-p1`，明确依赖 `1332176`
> P2-A1 分支：`codex/architecture-modernization-p2-app-factory`，分支末端 `bfc43fc`，主线合并提交 `0ec5508`
> P2-A2 分支：`codex/architecture-modernization-p2-http-auth-tests`，基点 `main@0ec5508`，最终 head `c0826e0`，PR #20 合并提交 `5b66004`
> P2-B 分支：`codex/architecture-modernization-p2b-role-boundary`，精确基点 `main@5b66004`，PR #21 merge commit `7c3e47ef1717a3fc498d63a1e3b7952e9100ecee`
> P2-C 分支：`codex/architecture-modernization-p2c-process-runtime`，精确基点 `main@7c3e47ef1717a3fc498d63a1e3b7952e9100ecee`，实现提交 `bc4d665`，[Draft PR #22](https://github.com/tony582/OnStarvoice/pull/22)
> 适用范围：StarVoice Extension、Node.js/Express 服务、PostgreSQL、Admin、Dashboard、测试与发布流程
> 性质：分阶段实施计划；P2-A1 的代码合并与限定生产发布已分别闭环，P2-A2 已进入主线且不需要部署，P2-B 已进入主线但未部署，P2-C 当前只是 Draft PR 候选；二者都不代表生产已获得角色锁、独立 Worker 或 split 拓扑

## 1. 决策摘要

StarVoice 需要进行架构优化，但不推倒重写，也不直接微服务化。

目标形态固定为：

> **模块化单体 + 独立后台 Worker + PostgreSQL 持久化任务/Outbox + 显式维护命令。**

继续保留同一仓库、同一数据库和现有外部协议。优先解决进程职责、依赖方向、持久化后台任务、测试门禁和发布可靠性，再拆分前端与 Extension 大文件。

2026-08-14 用户明确授权继续架构优化，P2-A1 收敛为提取不负责监听、数据库初始化和后台循环的 `createApp()`、保留兼容启动入口并补真实 HTTP 测试。P2-A1 最终通过 PR #19 合并至 `main@0ec5508`，再以不覆盖 hotfix 的 4 文件白名单单独上线。P2-A2 只扩展本地/隔离 PostgreSQL 的认证与租户 HTTP 测试及其保护/CI 门禁，已通过 PR #20 进入 `main@5b66004`，不需要生产部署。P2-B 随后通过 PR #21 合并为 `7c3e47ef1717a3fc498d63a1e3b7952e9100ecee`，但没有生产部署。用户随后授权 P2-C 本地实现，并单独授权提交、推送和创建 PR；实现提交 `bc4d665` 已进入 Draft PR #22。当前授权仍不包含合并、P2-D/P2-E 或任何生产操作。

### 1.1 当前授权矩阵

| 动作 | 当前状态 | 边界 |
|---|---|---|
| 方案文档与 P1-A 本地修改/验证 | 已授权 | 仅当前工作区和本地进程 |
| P1-B 本地修改/验证 | 已授权并完成 | 仅当前工作区、隔离空数据库与本地临时产物 |
| P2-A1 本地修改/验证 | 已授权并完成 | 仅 App 工厂、显式清理计时器、HTTP/契约测试与状态文档 |
| P2-A2 本地实施/验证 | 已授权并完成 | 仅测试、测试保护/运行器、CI 门禁和文档；最终 head `c0826e0` 的 push/PR 两组共 10 个任务全绿 |
| P2-A2 合并 | 已授权并完成 | PR #20 已合并，merge commit 为 `5b66004891d38de5e31a360f2b37190c6e623a46`；不需要生产部署 |
| P2-B 实施/验证、提交、推送、PR 与合并 | 已授权并完成 | 角色配置、版本化拓扑清单、PostgreSQL 角色锁、兼容入口接线、测试与文档已通过 PR #21 合并为 `7c3e47e` |
| P2-B 生产部署 | 未授权、未执行 | 进入 `main` 不自动获得生产配置或部署权限；线上仍不能声称已配置 `PROCESS_ROLE` 或角色锁 |
| P2-C 本地实施/验证 | 已授权并完成候选门禁 | 分支 `codex/architecture-modernization-p2c-process-runtime`；基点为 `main@7c3e47e` |
| P2-C 提交、推送与创建 PR | 已授权并完成 | 实现提交 `bc4d665` 已推送，并创建 Draft PR #22 |
| P2-C 合并或部署 | 未授权、未执行 | Draft PR #22 尚未合并，生产未部署；P2-D/P2-E 与另行发布授权仍是 split 上线前置条件 |
| P2-D 及后续阶段 | 未自动授权 | 需重新确认范围和 Gate；不得因 P2-C 本地候选存在而自动开始 |
| 创建 P2-A1 本地实施分支 | 已授权并完成 | `codex/architecture-modernization-p2-app-factory`，基点 `d8f3a1b` |
| P1 提交与推送 | 已授权并完成 | `codex/architecture-modernization-p1` 已对齐 `d8f3a1b`；该分支未单独建 PR，其内容后续随 PR #19 进入 `main` |
| P1 CI 兼容修复 | 已授权并完成 | Bash 修复已推送，远端 run `31611209688` 全绿 |
| P2-A1 提交 | 已授权并完成 | 独立提交 `1329da3`；随后以合并提交 `a795a1a` 同步生产基线 `236aa4d` |
| P2-A1 推送 | 已授权并完成 | `origin/codex/architecture-modernization-p2-app-factory` 已包含生产运行时兼容提交 `2634723`，未修改任何 hotfix 分支 |
| 合并 | 已授权并完成 | PR #19 已合并为 `0ec5508`；PR #20 已合并为 `5b66004`；PR #21 已合并为 `7c3e47e` |
| 测试环境发布 | 未授权 | 需单独范围和回滚点 |
| 生产只读核验 | 已授权并完成 | 限定发布后仍为 Node `18.20.8`、迁移 `066_tenant_comment_risk_attention.sql`、Extension `0.3.83`；Chrome/Edge Agent 心跳与公网/内网健康已核对 |
| P2-A1 生产部署 | 已授权并完成 | 生产中纳入 Git 的 Server 文件是 `236aa4d` 完整基线叠加来自 `0ec5508` 的 4 个文件，不是完整 `0ec5508` 树；未新增迁移/依赖，也未发布前端或 Extension |
| 生产迁移或数据操作 | 未授权且不需要 | P2-A1 未单独执行手工迁移、数据修复或业务任务；正常启动初始化与既有后台循环按原配置恢复 |
| 客户 Extension 升级与 Reload | 本批不需要 | 保持 `0.3.83`；只重建并核对本地加载快照，不触发业务任务 |

## 2. 基线与当前状态

### 2.1 P1-A 前审查基线

| 项目 | 2026-08-12 审查结果 |
|---|---|
| 审查分支 | `codex/edit-unattended-plans`，比 `origin/main` 多 5 个提交 |
| 审查时工作区 | 审查结束时干净 |
| Extension | `0.3.80` |
| 最新迁移 | `063_social_agent_daily_health.sql` |
| Node 测试 | 1133/1133 通过 |
| Admin | 构建通过；lint 288 个存量错误 |
| Dashboard | 因 TypeScript `baseUrl` 弃用错误无法构建；lint 16 个错误 |
| CI | 仓库内没有 CI 工作流 |
| 服务端结构 | 30 个路由文件中约 27 个直接访问数据库 |
| 核心文件 | `sidebar-logic.js` 约 29,559 行；`capture-sync.js` 约 18,103 行；`background.js` 约 10,780 行；`capture-cloud.js` 约 9,510 行 |
| 文档一致性 | README 为 `0.3.80/063`，部分交接文档仍为 `0.3.76/054` |

### 2.2 P1 本地实施记录及后续闭环

| 项目 | 当前事实 |
|---|---|
| 实施分支 / 基点 | `codex/architecture-modernization-p1` / `1332176` |
| 实施当时与 `main` 的关系 | 当时明确依赖尚未合并到 `main` 的 5 个功能提交 |
| P1 本地提交 | Dashboard `2c32733`；工程门禁 `39c9abe`；方案 `61263ca`；Bash 修复 `2a51a7d`；远端证据 `d8f3a1b` |
| 远端分支 | `origin/codex/architecture-modernization-p1` 已对齐 `d8f3a1b`；该分支未单独创建 PR，其内容后续随 PR #19 进入 `main` |
| 已修改/新增范围 | 方案与运行文档、`.nvmrc`、CI、服务端测试/语法入口、仓库卫生与 Extension 快照门禁、PostgreSQL/HTTP 集成测试约定、Dashboard TS/lint 修复与 lockfile 对齐、Admin lint 基线脚本 |
| 当前验证 | Node 24.12.0 的全新临时副本：1133/1133、Admin build、Dashboard build/lint、Admin lint 288/0 基线、服务端语法、仓库卫生、Extension 快照和 `git diff --check` 通过，检查前后源码指纹一致 |
| 真实数据库证据 | 本地 PostgreSQL 17.9 空库上的非 reset 迁移连续执行两次幂等；5 个 reset 迁移均跳过；测试库已删除并确认不存在 |
| Dashboard lockfile | `npm ci` 曾发现 `@emnapi/wasi-threads` 锁定元数据不一致；仅对齐 package-lock，未修改 `package.json`，全新副本已可重复 `npm ci` |
| 锁定依赖状态 | 三个子项目 `npm ci` 均通过；Server 扫描为 0，Admin 报告 1 个中危/4 个高危，Dashboard 报告 3 个高危；本批不执行自动升级 |
| 首轮远端 CI | [Actions run 31609240011](https://github.com/tony582/OnStarvoice/actions/runs/31609240011)：PostgreSQL 14/16 均通过；质量任务在测试入口因 Ubuntu 缺少 `zsh` 以 127 退出，业务测试未启动 |
| 修复后远端 CI | [Actions run 31611209688](https://github.com/tony582/OnStarvoice/actions/runs/31611209688)：质量任务、PostgreSQL 14 与 PostgreSQL 16 全部通过，G1 关闭 |
| 后续闭环 | P1/P2-A1 内容已经 PR #19 进入 `main@0ec5508`；P2-A1 只按 4 文件白名单限定发布；P2-A2 已由 PR #20 进入 `main@5b66004` 且不需要生产发布；P2-B 从该精确基点独立开发并由 PR #21 合并为 `7c3e47e`；P2-C 再从该 merge commit 建立本地分支 |

P1 审查时的测试数量是重要资产，但当时 118 个测试文件中有 86 个包含读取源码文本的契约断言。它们能够防止大量接口漂移，不能替代真实 Express、PostgreSQL、浏览器和多进程竞态测试。

### 2.3 P2-A1 实现与门禁闭环（2026-08-14）

| 项目 | 当前事实 |
|---|---|
| 分支 / 合并 | `codex/architecture-modernization-p2-app-factory`；原基点 `d8f3a1b`；P2 提交 `1329da3`；生产基线合并 `a795a1a`；分支末端 `bfc43fc`；PR #19 合并提交 `0ec5508` |
| 生产修复隔离 | P2-A1 发布前，生产、发布 marker 与 27 个关键文件均确认对应 `236aa4d`；实施分支仅同步该已部署提交，不吸收未提交的 heartbeat-hardening 工作区 |
| App 边界 | 新增 `server/app.js`；只组装 Express、中间件、静态资源、路由与错误处理，不监听端口、不初始化数据库、不启动 Cron 或后台循环 |
| 兼容入口 | `server/index.js` 继续保持数据库初始化、媒体目录、Cron、收尸、回填、AI 循环、HTTP 监听和退出信号职责 |
| 导入副作用 | Verify 限流清理与 ASR 临时媒体清理由模块级计时器改为显式、幂等的启动/停止函数；兼容入口负责启动和停止 |
| HTTP 证据 | 临时本地端口真实验证健康、根重定向、缺失认证、缺失租户凭据、API 404、CORS 允许/拒绝和 JSON 500；用例固定使用不可达本地测试库并阻止非本地 HTTP 请求，证明这些拒绝/健康路径不依赖真实数据库或外部 HTTP 服务 |
| P2 独立门禁 | Node `24.12.0`：1144/1144；Admin lint 288/0 基线与 build；Dashboard lint/build；服务端语法；仓库卫生；92 文件 Extension 快照均通过 |
| 组合完整门禁 | 同步 `236aa4d` 后 Node 24.12.0 为 1180/1180；Admin lint 288/0 基线与 build、Dashboard lint/build、服务端语法、仓库卫生及 92 文件 Extension 快照均通过 |
| 生产 Node 兼容 | 根级 `package.json` 显式声明 Extension 源码为 ESM；生产同版 Node 18.20.8 的全量回归 1180/1180、Server 语法、397 项 Server 相关回归、97 项 P2 直接回归和隔离 PostgreSQL 17 迁移 1/1 均通过；真实 `server/index.js` 隔离启动冒烟覆盖健康、重定向、认证拒绝、CORS 与 SIGTERM 关闭 |
| 远端组合门禁 | PR 最终 run `31791498224` 的 Node 24 质量、生产 Node 18 兼容、PostgreSQL 14 与 PostgreSQL 16 四类任务全部通过 |
| 明确未做 | 未增加 `PROCESS_ROLE`、未拆 Worker、未改 Cron/业务路由/数据库迁移/健康语义；P2-A2 只补测试证据，不实现 readiness 或角色拆分 |

### 2.3.1 P2-A1 限定生产发布记录（2026-08-14）

生产发布标识为 `p2-app-factory-0ec5508-20260814-182006`。发布前的完整应用基线是 `236aa4da7520da3359ab13f98ab9b761ca6a0d53`；本次仅用 `0ec5508ecd5f1c28093dc3f974799adb3fa0ee62` 中的下列 4 个文件覆盖对应 Server 运行面：

| 生产文件白名单 | 发布时核对的 SHA-256 |
|---|---|
| `server/app.js` | `abbc56725029e47ab9a88b9b60b01ffaab752dd24b6145695f95604d5dee3cb5` |
| `server/index.js` | `2780e3e9ccde4af09c02a2a4e075eccd42719abbcba219da4d698e07b8c22386` |
| `server/routes/verify.js` | `f7fc1abe31883e420160a3a8029a151bcf10c661a5b40c110f9f838243e8cf75` |
| `server/services/asr-media-host.js` | `b66410e33db74a49c060de2876ac45c25bee636411eb236de7e59ff704e2d6e6` |

因此，生产中纳入 Git 的 Server 文件精确组成是 **`236aa4d` 完整基线 + 上述来自 `0ec5508` 的 4 文件**，不是完整 `main@0ec5508` 工作树。生产 `RELEASE_COMMIT=0ec5508...` 只是这四个文件的来源与发布审计标记，不能用它推导其他文件已上线。

- 本次未发布根级 `package.json`、CI、文档、测试、依赖、数据库迁移、Admin、Dashboard 或 Extension。
- 生产仍使用 Node `18.20.8`，最新迁移仍为 `066_tenant_comment_risk_attention.sql`，Extension 仍为 `0.3.83`。
- 本地 `extension-build` 已备份并重建为生产 0.3.83 快照（92 文件、生产 API、无禁入文件）；线上 ZIP SHA-256 为 `00ef567ceeb43ad36312bdab9bd7e947c8bba73fdf2e003f17d6943c1eba1165`，Chrome/Edge 13 个 Agent 心跳均为 0.3.83。
- Server 完整回滚目录为 `/opt/onstarvoice/backups/server-before-p2-app-factory-0ec5508-20260814-182006`；发布前数据库备份为 `/opt/backups/onstarvoice/onstarvoice-before-p2-app-factory-0ec5508-20260814-182006.dump`（`295676547` bytes、SHA-256 `35038a5417424ae1c90846d3fd8e3aaab1507505bf142ec2086a29f7fbbf1b97`、`pg_restore --list` 已复核）。
- 原子切换与 PM2 重启后，内网/公网健康、Admin、未认证请求拒绝、迁移版本与 Chrome/Edge Agent 心跳均已核对；这些证据只支持 P2-A1 限定发布稳定，不提前关闭 G2。
- 本批没有登录生产账号做租户切换或新建一次性业务任务；替代验证及跳过原因已记录，不能据此宣称完整端到端业务冒烟通过。

### 2.3.2 P2-A2 认证与租户 HTTP 测试门禁（2026-08-14）

| 项目 | 已验证事实 |
|---|---|
| 范围 | 只修改测试、测试保护/运行器、CI 和文档；没有生产运行时代码、迁移、依赖、前端或 Extension 变更 |
| 真实 HTTP | 在隔离 PostgreSQL 上覆盖登录、Cookie/Bearer 会话、成员默认租户、显式租户、跨租户 403、激活码租户路由、注销与已撤销会话 401 |
| 数据安全 | Runner 与每个数据库 integration 共用 fail-closed 校验；只接受本机 `onstarvoice_ci*`/`onstarvoice_test*`，拒绝冲突 URL 与可覆盖主机的查询参数；直跑迁移测试也在任何 `[DB]` 行为前失败 |
| 本地门禁 | Node `18.20.8` 与 `24.12.0` 全量回归均为 1182/1182；两版隔离 PostgreSQL 17 integration 均为 2/2，fixture 清理后为 0 |
| 远端门禁 | PR #20 最终 head `c0826e0` 的 [push run 31801832407](https://github.com/tony582/OnStarvoice/actions/runs/31801832407) 与 [PR run 31801835798](https://github.com/tony582/OnStarvoice/actions/runs/31801835798)：质量、生产 Node 18、PG14/Node24、PG16/Node24、PG16/Node18 两组共 10 个任务全部通过 |
| 合并/发布结论 | PR #20 已合并为 `5b66004`；该批不改变生产运行面，不需要生产部署；其证据关闭 G2 的 HTTP 认证/租户边界技术项 |

### 2.3.3 P2-B 进程角色与互斥合并记录（2026-08-15）

| 项目 | 已验证事实 |
|---|---|
| 分支 / 基点 | `codex/architecture-modernization-p2b-role-boundary` / `main@5b66004891d38de5e31a360f2b37190c6e623a46` |
| 显式角色 | 定义 `all`、`api`、`scheduler`、`ai-media`、`maintenance`；生产缺少、留空、组合或未知 `PROCESS_ROLE` 时在数据库、Cron 和 HTTP 之前失败；当前兼容入口只允许 `all` |
| 期望拓扑 | `deploy/process-topology.production.json` 只允许一个 `all` 实例；CI 运行离线 checker，混合、多 `all`、重复执行权与当前不可部署的 split 清单均被拒绝。该 JSON 不读取真实 PM2 或线上环境，不能作为生产拓扑证据 |
| 角色互斥 | `all` 使用一个独立 PostgreSQL 会话，在 `initDb()` 前同时持有 Scheduler 与 AI/Media 两把 advisory lock；抢锁失败不会继续迁移、媒体初始化、Cron、后台循环或 HTTP 监听 |
| 锁丢失 | 锁会话异常后兼容进程立即非零退出。P2-B 合并快照中的 Cron 与递归计时器尚无完整停止句柄，因此该批不宣称优雅降级或在途工作零重叠；该能力由后续 P2-C 本地候选承接 |
| 本地门禁 | Node `18.20.8` 与 `24.12.0` 全量回归均为 1205/1205；两版隔离 PostgreSQL 完整 runner 均为 12/12；Admin lint 288/0 基线与 build、Dashboard lint/build、两版 Server 语法、仓库卫生、版本化拓扑和 92 文件 Extension 快照均通过 |
| 远端门禁 | PR #21 的实现 head `c2b01e5` 已通过 [push run 31815286265](https://github.com/tony582/OnStarvoice/actions/runs/31815286265) 与 [PR run 31815346416](https://github.com/tony582/OnStarvoice/actions/runs/31815346416)，两组质量、生产 Node 18、PG14/Node24、PG16/Node24、PG16/Node18 共 10 个任务全部通过 |
| 明确未做 | 未建立独立 API/Worker 入口，未新增 readiness、迁移锁/checksum、迁移、依赖、业务路由、Admin、Dashboard 或 Extension 运行时变更；未修改生产 `.env`、PM2、数据库或文件 |
| 交付状态 | PR #21 已于 2026-08-15 合并，merge commit 为 `7c3e47ef1717a3fc498d63a1e3b7952e9100ecee`；尚未部署，生产发布仍须另行授权 |

### 2.3.4 P2-C 独立进程运行时候选（2026-08-15，本地）

| 项目 | 当前事实 |
|---|---|
| 分支 / 基点 | `codex/architecture-modernization-p2c-process-runtime` / `main@7c3e47ef1717a3fc498d63a1e3b7952e9100ecee` |
| 交付状态 | 实现提交 `bc4d665` 已推送并创建 Draft PR #22；未合并、未部署 |
| 独立入口 | 已建立专用 `api`、`scheduler`、`ai-media` 入口；API-only 不拥有周期 Worker，Worker-only 不监听公网 HTTP |
| 健康语义 | 保留兼容 `/api/health`，增加 `/api/health/live` 与 `/api/health/ready`；readiness 包含数据库可用性，liveness 不因单次数据库探测失败而被混同为进程死亡 |
| 停止顺序 | Cron、递归 timer 与 HTTP listener 均有停止句柄；所有刻意晚于 HTTP 响应或 Cron 回调完成的进程内后台工作进入统一 registry。普通信号先停止接收新工作，再有界 drain 运行时与该 registry，在普通数据库连接关闭后最后释放角色锁；任一 drain 不完整时保留数据库与角色锁直到进程退出，锁会话丢失仍 fail-closed 立即退出 |
| 数据库边界 | 独立角色只读确认数据库可连接，并把仓库内全部非 reset 迁移文件与 `schema_migrations` 做全集核对；缺少任一版本即拒绝启动，不执行迁移或 bootstrap；兼容 `all` 入口继续承担现有初始化行为 |
| 拓扑清单 | `deploy/process-topology.split.candidate.json` 恰好包含 `api`、`scheduler`、`ai-media` 各 1 个实例；`deploy/process-topology.production.json` 仍是单一 `all`，默认 checker 继续拒绝 split，只允许显式 candidate 校验 |
| 本地回归与 PostgreSQL | Node `24.12.0`、`18.20.8` 全量回归各 1250/1250；隔离 PostgreSQL 完整 runner 在两版 Node 下各 13/13，覆盖非 reset 迁移全集幂等、独立角色、真实多进程锁、split 启停与 drain |
| 其余工程门禁 | Node 24/18 Server 全部 `.js` 语法、repository hygiene 615 files、生产 `all:1` 拓扑、candidate-only split 三角色各 1、Admin lint 288 errors/0 warnings 存量上限与逐文件门禁、Admin build、Dashboard lint/build、92 文件 Extension production snapshot 均通过；Admin 的既有 Vite native-config/chunk warning 非失败；全部门禁前后 `git status` 一致 |
| 上线阻断 | 历史 repair/backfill one-shot 仍是 `all`-only，尚未确定 split owner；迁移治理 P2-D、测试环境切换演练 P2-E 与另行部署授权都未完成，因此 split 不可上线 |

### 2.4 已经做对的部分

- 租户、Agent、Task、Attempt、Work Item、Event 的核心模型应继续保留。
- 弹性队列已经使用事务、行锁、`SKIP LOCKED`、`execution_task_id` 和 `assignment_revision` 防止重复领取及旧执行回写。
- Extension 本地账本、稳定任务 ID、检查点、恢复预算和云端镜像具有大量业务回归测试。
- 原始采集与 AI 研判分离，AI 失败不回滚原始数据的原则正确。
- 快照追加、任务历史留痕、租户隔离和 Agent 能力检查是后续改造必须保持的基础。

### 2.5 当前主要矛盾

业务能力持续增长，但新的领取、接力、恢复、排期和统计逻辑仍大量进入 HTTP 路由和单一服务进程。系统现在能够承载更多业务，却更难安全修改、独立扩容和准确判断健康状态。

## 3. 优化原则

### 3.1 必须保持的不变量

1. 同一租户、Agent 和稳定任务 ID 的重试不能产生重复业务任务。
2. Work Item 同一时刻只能有一个有效执行者；重新分配必须保留历史。
3. `execution_task_id`、`assignment_revision`、请求哈希和任务检查点的栅栏语义不得弱化。
4. Extension 与服务端现有命令、心跳、快照和同步协议保持向后兼容。
5. 租户 ID 必须贯穿查询、锁、事件、Job 和日志上下文。
6. 原始采集成功不得因 AI、通知、报表或媒体处理失败而回滚。
7. 删除、无结果、范围外与平台安全验证必须继续使用不同状态语义。
8. 快照和任务事件继续只追加；业务历史不能被技术压缩误删。
9. 数据库迁移优先采用向前兼容的加法迁移，应用回滚不得依赖破坏性 down migration。
10. 每一个实施阶段必须可独立验收、暂停和回滚。

### 3.2 实施纪律

- 机械移动与行为改变分开提交，避免无法判断回归来源。
- 路由兼容层保留到新模块经过测试和灰度后再删除。
- 一个阶段允许包含多个小 PR，不允许把跨阶段改动合成一个大 PR。
- 每次只改变一个主要风险面：门禁、启动边界、任务持久化、前端注册或 Extension 模块化。
- 新增数据库字段、表和索引必须有真实 PostgreSQL 测试及查询计划检查。
- 发布使用明确文件清单；不使用 `git add -A` 混入构建物、ZIP、`.env*` 或无关改动。

### 3.3 明确非目标

本轮架构优化不包括：

- 拆分微服务、引入 Kubernetes、Kafka 或新的基础设施集群；
- 增加生产机器或 PM2 实例数；
- 调整无人值守时间、任务频率、Agent 分配策略或平台访问节奏；
- 增加自动重试次数；
- 更换 AI 提供商、模型、路由、提示词或增加 AI 消耗；
- 改变客户可见工作流或进行视觉改版；
- 清理生产任务、修改生产数据或自动部署；
- 为了“代码更漂亮”而重写已验证的采集业务规则。

## 4. 目标架构

```text
Extension / Admin / Dashboard
              │
              ▼
      API 进程（HTTP 边界）
      ├─ 认证、租户和参数校验
      ├─ 调用应用用例
      └─ 返回稳定 API 契约
              │
              ▼
          PostgreSQL
      ├─ 业务事实与历史
      ├─ Task / Attempt / Work Item / Event
      ├─ Job / Outbox / Lease
      └─ 迁移与维护审计
          ▲             ▲
          │             │
 Scheduler Worker    AI / Media / Report Worker
 排期、租约回收、     持久化领取、重启续跑、
 命令结算、通知领取   有界重试、幂等完成

 Maintenance CLI
 ├─ migrate
 ├─ repair / backfill
 └─ preview / verify
```

### 4.1 模块依赖方向

```text
routes / worker entrypoints
        ↓
application use cases
        ↓
domain policies and state transitions
        ↓
repositories / external adapters
        ↓
PostgreSQL / AI / SMTP / filesystem
```

Cron、Worker 和路由可以调用同一个应用用例；Cron 不再从路由文件导入业务函数。领域层不得依赖 Express 的 `req/res`。

### 4.2 进程角色

| 角色 | 负责 | 不负责 |
|---|---|---|
| API | HTTP、鉴权、租户、参数、同步调用 | Cron、历史回填、无限循环、一次性 repair |
| Scheduler Worker | 排期、命令对账、租约回收、通知领取 | 对外 HTTP、页面静态资源 |
| AI/Media Worker | AI、媒体、报告等持久化 Job | 修改采集协议、直接决定 Agent 分配 |
| Maintenance CLI | 迁移、repair、backfill、preview、verify | 随服务启动自动长期运行 |

P2 第一版仍保留 `PROCESS_ROLE=all` 兼容模式，直到独立角色通过测试和灰度；不能一次性切断现有启动方式。但旧拓扑和拆分拓扑必须二选一，严禁同时运行 `all` 与任一独立 Worker。

| 启动拓扑 | 允许组合 | 禁止组合 |
|---|---|---|
| 兼容拓扑 | 仅 `all` | `all` + Scheduler/AI/Media Worker |
| 拆分拓扑 | API + 所需独立 Worker | 任意 `all` 进程 |

本地开发可显式选择兼容拓扑；生产缺少明确角色配置时必须拒绝启动，不能静默回落到 `all`。

跨进程互斥采用两层防线：发布清单预检拒绝混合拓扑；每个拥有 Scheduler 或 AI 执行权的进程还必须持有对应 PostgreSQL advisory lock/角色租约。仅检查本进程的环境变量不足以证明互斥；锁丢失时必须停止领取并退出或降为不可就绪。

## 5. 总体路线图

| 阶段 | 目标 | 主要风险 | 参考工作量 | 前置门槛 |
|---|---|---|---|---|
| P0 | 固定基线、边界和实施账本 | 在未合并功能分支上产生长期分叉 | 0.5–1 天 | 无 |
| P1 | 建立可重复工程门禁 | CI 被存量 lint 债永久阻塞 | 2–4 天 | G0 |
| P2 | 拆开 App、进程、配置、健康与迁移边界 | 启动行为变化影响生产 | 4–7 天 | G1 |
| P3 | 从路由抽离采集任务核心 | 状态机或事务语义漂移 | 7–12 天 | G2 |
| P4 | 建立持久化 Job/Outbox 和独立 Worker | 重复执行、增加 AI 调用 | 7–12 天 | G3 |
| P5 | 完善可观测性与可回滚发布 | 误把进程在线当业务健康 | 4–7 天 | G4 |
| P6 | 统一 Admin 页面与 API 边界 | 权限、桌面/移动展示漂移 | 5–10 天 | G2；可独立开发，但上线不得与 P4 切换同窗 |
| P7 | 渐进拆分 Extension 大文件 | 隐含平台规则和消息协议回归 | 按平台持续推进 | G3、真实浏览器环境 |
| P8 | 删除兼容层和遗留入口 | 过早删除仍在使用的路径 | 2–5 天 | 前述阶段稳定后 |

参考工作量用于排序和控制批次，不是发布日期承诺。默认关键路径为 `P0 → P1 → P2 → P3 → P4 → P5 → P7 → P8`；P6 在 G2 后可独立开发，但不能与 P4 执行权切换同一发布窗口。每一 Gate 通过后只表示技术就绪，下一阶段仍需明确授权，不按日历或 Gate 状态自动推进。

## 6. 分阶段实施计划

## P0：固定实施基线

### 目标

避免架构改造与当前“弹性队列/无人值守计划”功能分支相互覆盖，并保存可比较的验证基线。

### 工作包

- **P0-1 基线证据**：记录当前提交、分支、文件规模、测试、构建、lint 和迁移版本。
- **P0-2 分支决策**：确认当前 5 个提交是先合并 `main`，还是作为架构分支的明确依赖。
- **P0-3 ADR**：记录“模块化单体 + 独立 Worker，不微服务化”的架构决策。
- **P0-4 执行账本**：在本文维护阶段状态、已完成验证、偏差和未决事项。

### Gate G0

- [x] 当前分支和工作区已核对。
- [x] 架构审查与实施计划已完成。
- [x] 当前 5 个功能提交作为架构分支的明确依赖；未宣称已进入 `main`。
- [x] 实施分支固定为 `codex/architecture-modernization-p1`，基点 `1332176` 可复现。
- [x] 本文只记录当前本地证据，未把生产状态当作本地分支事实。

### 回滚

P0 不改变运行行为。需要撤销时，删除本地实施分支及未提交计划文件即可；架构方向、基点和授权记录属于审计事实，不以删除历史方式“回退”。若这 5 个依赖提交最终不进入 `main`，后续实现必须重新基于获批基点重放，并重跑 G0/G1。

## P1：工程门禁与可重复构建

### 目标

先建立能够约束后续重构的自动检查；本阶段不改变采集、调度、AI 和生产运行行为。

### 工作包

1. **P1-1 修复 Dashboard 构建**
   - 处理 TypeScript `baseUrl` 弃用错误；
   - 运行 Dashboard build 和 lint；
   - 不改 Dashboard 指标、查询和视觉结构。
2. **P1-2 固定运行环境**
   - 明确仓库 Node 主版本；
   - 使用 lockfile 和 `npm ci` 验证各独立包；
   - 服务端增加标准 `test` 脚本，避免只靠人工拼命令。
3. **P1-3 CI 工作流**
   - 全量 Node 测试；
   - Admin、Dashboard 构建；
   - 服务端语法检查；
   - Extension 源码/交付快照一致性检查；
   - `git diff --check` 和敏感文件/构建物检查。
4. **P1-4 lint 过渡门禁**
   - Dashboard 先清零；
   - Admin 记录 288 个基线错误；
   - CI 同时禁止 Admin 错误/警告总数与逐文件数增加，避免新债被其他文件的减少掩盖；
   - 按模块减少存量，不通过全局关闭规则伪造通过。
5. **P1-5 最小真实测试骨架**
   - 建立 `createApp()` 后续测试所需的 HTTP 目录与隔离约定；
   - 建立临时 PostgreSQL 测试库约定；
   - 此阶段可先搭骨架，不改运行入口。

### Gate G1

- [x] Node 全量测试通过且数量不低于基线 1133（本地 Node `24.12.0`，2026-08-12：1133/1133）。
- [x] Admin 和 Dashboard 均可重复构建（本地 Node `24.12.0`，2026-08-12）。
- [x] Dashboard lint 为 0；Admin 不新增 lint 错误（基线检查：288 errors / 0 warnings）。
- [x] 远端 CI 在获批提交的干净 checkout 上执行成功（2026-08-12，run `31611209688`）。
- [x] CI 配置不需要真实生产密钥、生产数据库或客户浏览器；本地隔离测试已验证。
- [x] 仓库源码到本地 `extension-build/` 交付快照的一致性门禁通过；未访问生产、客户浏览器或外部系统。

G1 已关闭：首轮远端 CI `31609240011` 暴露 Ubuntu 缺少 `zsh` 的入口兼容问题；`2a51a7d` 将快照链路改为 Bash 兼容，`d8f3a1b` 补齐证据。2026-08-12 远端干净 checkout run `31611209688` 的质量任务与 PostgreSQL 14/16 矩阵全部通过。该结论只证明 P1 工程门禁可重复执行，不代表架构分支已合并或部署。

干净 checkout 与 CI 的关闭证据已经固定在上述两个 run 中。本地补充验证不能冒充远端 CI。Extension 门禁仅执行仓库内 `scripts/sync-extension-build.zsh production` 并检查生成结果；`extension-build/` 是本地忽略目录，不是线上或客户快照。

### 回滚

- CI、脚本、TypeScript 配置可独立回滚。
- 不包含数据库迁移，不改变运行时协议。
- 如果 lockfile 变化超出最小依赖范围，停止并单独审查，不强制升级。

## P2：应用启动、进程、配置、健康和迁移边界

### 目标

让 HTTP 服务可以被测试和独立启动；让迁移、repair 和后台循环不再隐式绑定到 API 进程。

### 工作包

1. **P2-1 `createApp()`**
   - 将 Express 构建提取为无监听、无 Cron、无迁移、无回填副作用的工厂；
   - `server/index.js` 暂时作为兼容入口；
   - API 路径、静态资源和错误响应保持兼容。
2. **P2-2 进程入口**
   - 建立 API、Scheduler Worker、AI/Media Worker、Maintenance CLI 入口；
   - 开发环境可显式选择 `PROCESS_ROLE=all`，生产必须显式选择兼容或拆分拓扑；
   - 启动时检测角色互斥，`all` 与任何独立 Worker 并存时拒绝启动；
   - 每种角色支持独立优雅退出。
3. **P2-3 配置校验**
   - 区分开发默认值与生产必填配置；
   - 生产环境缺少 `DATABASE_URL`、关键目录或认证配置时明确失败；
   - 日志不得输出密钥和完整连接串。
4. **P2-4 健康端点**
   - `live`：进程能响应；
   - `ready`：数据库可用、迁移状态可读、角色初始化完成；
   - Worker 单独记录最后成功循环、最近错误和积压年龄；
   - 不再用单一 `/api/health` 推断 AI 或队列吞吐健康。
5. **P2-5 迁移治理**
   - 增加数据库级迁移锁；
   - 保存迁移 checksum，拒绝已执行文件被静默修改；
   - reset/repair/backfill 转入显式维护流程；
   - 新维护任务不再复用 `schema_migrations` 冒充结构迁移。

### P2 子阶段顺序

1. **P2-A：可测试 App 工厂**：只提取 `createApp()` 与 HTTP 测试，不改变生产入口和后台循环。
2. **P2-B：角色配置与互斥**：加入显式角色校验、发布清单预检和数据库角色锁；继续使用兼容 `all` 拓扑。
3. **P2-C：独立入口与健康**：建立 API/Worker 入口、liveness/readiness 和优雅退出，先在本地多进程测试。
4. **P2-D：迁移治理**：迁移锁、checksum 和维护命令单独实施，不与拓扑切换同一批。
5. **P2-E：测试环境拓扑切换演练**：获得测试环境授权后才执行 `all → split → all` 演练。

每个子阶段独立提交候选、独立验证和独立回滚；前一子阶段通过不自动授权下一子阶段。

### Gate G2

- [x] `createApp()` 的真实 HTTP 测试覆盖认证、租户、404、500 和健康端点（P2-A1 覆盖拒绝/错误路径；P2-A2 以隔离 PostgreSQL 覆盖成功登录、会话、默认/显式租户、跨租户拒绝、激活码路由与注销；PR #20 已合并为 `5b66004`）。
- [x] API-only 模式不会启动 Cron、AI 循环、repair 或历史回填（P2-C 本地候选的职责测试与真实 split PostgreSQL runner 已通过）。
- [x] Worker-only 模式不监听公网 HTTP（P2-C 本地候选的职责测试与真实 split PostgreSQL runner 已通过）。
- [x] API 与 Worker 同时启动时，不重复注册同一调度循环（Scheduler 与 AI/Media 各持自身角色锁，split candidate 恰好三角色各 1，真实多进程 runner 已通过）。
- [x] API 保留兼容 `/api/health`，并以 `/api/health/live`、`/api/health/ready` 区分存活与数据库/迁移就绪；数据库不可用不会被误写成进程死亡。
- [x] 普通信号停止新工作并有界 drain Cron、递归 timer、HTTP 在途请求及响应后后台任务；只有全部 drain 且普通数据库连接关闭后才最后释放角色锁，失败则保留锁直至进程退出。
- [x] 独立角色对全部非 reset 迁移做只读全集校验，缺失任一版本即拒绝启动，且不运行迁移或 bootstrap。
- [x] `all` 与独立 Worker 的混合拓扑会被版本化期望清单校验拒绝（P2-B 已通过 PR #21 进入主线；该清单不读取真实 PM2，未来发布仍须独立核对实际实例和有效环境）。
- [x] 真实并发进程证明 Scheduler/AI 数据库角色锁只有一个持有者，锁丢失者停止领取（P2-B 已通过 PR #21 进入主线，使用同一专用 PostgreSQL 会话持两把锁；当前安全处置是立即退出进程）。
- [x] 生产缺少显式角色配置时失败，不静默回落到 `all`（P2-B 已通过 PR #21 进入主线，但尚未部署，因此生产尚不具备该能力）。
- [ ] 两个并发迁移进程只有一个执行迁移，另一个安全等待或退出。
- [ ] 修改已执行迁移文件可被 checksum 门禁发现。
- [x] 兼容 `all` 模式通过当前 P2-C 本地候选的组合回归基线（Node 18/24 各 1250/1250、隔离 PostgreSQL runner 各 13/13）；P2-B 合并前实现 head `c2b01e5` 的 push/PR 两组远端任务也保持全绿。

G2 **整体仍未关闭**：P2-C 的实现与本地门禁已经完成，并已进入 Draft PR #22，但尚未合并或部署；并发迁移锁与迁移 checksum 属于 P2-D，`all → split → all` 测试环境演练属于 P2-E，历史 one-shot 也尚无 split owner。上面的勾选只表示候选技术证据成立，不表示 split 已具备发布资格或生产已部署该能力。

### 灰度与回滚

- 第一轮生产仍可显式使用兼容 `all` 模式；独立角色先在本地和测试环境验证，两种拓扑不得并存。
- 保留旧入口一个完整发布周期。
- 数据库变化只允许加法迁移；旧应用必须仍能读取新结构。
- `all → split` 顺序：阻止新工作进入旧循环，等待/结算在途工作，停止 `all` 并确认进程退出和角色锁释放，启动独立角色并确认唯一锁持有者与 readiness，最后恢复新工作入口。
- `split → all` 回滚顺序：阻止新工作，停止全部独立 Worker 并确认锁释放，停止 API，启动兼容 `all` 并确认唯一角色锁与 readiness，再恢复新工作；任何时刻不得重叠执行权。

## P3：采集任务领域边界

### 目标

把任务领取、命令、租约、状态迁移和事件投影从 HTTP 路由中移到可复用、可测试的模块，同时保持现有协议和数据库语义。

### 建议目录

```text
server/modules/capture/
  domain/
    task-status.js
    recovery-policy.js
    assignment-fence.js
  application/
    heartbeat.js
    command-lifecycle.js
    elastic-work.js
    lease-reconciliation.js
    snapshot-projection.js
  infrastructure/
    capture-repository.js
    event-repository.js
  http/
    capture-router.js
```

目录名可在实施时微调，依赖方向不能反转。

### 工作包

1. **P3-1 后台函数先出路由**
   - 迁移 `reconcilePendingCaptureCommands`、`reconcileElasticCaptureLeases`、自动恢复等函数；
   - Cron 改为依赖应用服务；
   - 原路由保留临时 re-export，避免一次性断开测试和旧导入。
2. **P3-2 Heartbeat 用例**
   - 输入为已鉴权 Agent 与规范化心跳 DTO；
   - 统一完成 Agent 更新、快照投影、弹性领取和命令返回；
   - 不接收 Express `req/res`。
3. **P3-3 弹性领取用例**
   - 保留现有行锁、`SKIP LOCKED`、能力、平台、空闲和安全阻断判断；
   - 领取、子任务、Command、Attempt、Item revision 和 Event 在同一事务内完成。
4. **P3-4 Repository 边界**
   - 收敛 SQL 和事务接口；
   - 不建立无业务意义的“一表一个 Repository”；
   - 以用例和事务边界组织查询。
5. **P3-5 状态转换策略**
   - 把状态合法性、聚合和恢复处置提取为纯函数；
   - 数据库仍保留约束作为最后防线。

### Gate G3

- [ ] 旧 API 请求与响应契约快照不变。
- [ ] 两个并发 Agent 领取同一池时，每个 Work Item 只有一个有效领取者。
- [ ] 旧 `assignment_revision`、旧 `execution_task_id` 和旧 Attempt 的写回全部被拒绝。
- [ ] Command 过期、Agent 离线和父任务取消竞态有真实 PostgreSQL 测试。
- [ ] Cron 不再从路由目录导入业务函数。
- [ ] 路由只保留鉴权、参数、状态码和应用用例调用。
- [ ] 在隔离测试库装载脱敏代表数据后，关键查询执行计划满足切换前记录的量化阈值；任何生产只读对比另行授权。

### 灰度与回滚

- 先做纯移动和兼容导出，再切换调用者，最后删除旧实现。
- 每个子用例单独 PR；不得一次移动完整 9,000 行文件。
- 任一行为对比不一致时恢复旧调用路径，不修改生产任务状态补偿差异。

## P4：持久化后台 Job 与独立 Worker

### 目标

消除 `setImmediate`、内存 `Set/Map` 和启动后定时回填导致的重启丢任务问题；后台工作可以领取、续租、失败重试和幂等完成。

### 最小 Job 模型

| 字段 | 用途 |
|---|---|
| `tenant_id` | 租户隔离；系统任务必须使用明确系统作用域 |
| `kind` | Job 类型 |
| `payload` | 最小引用，不保存密钥和大段客户内容 |
| `idempotency_key` | 业务幂等键 |
| `status` | queued / leased / succeeded / retry_wait / dead |
| `attempt_count` | 有界尝试次数 |
| `run_after` | 延迟与退避 |
| `lease_owner/lease_expires_at` | 崩溃回收 |
| `lease_revision` | fencing token；拒绝旧租约持有者续租或完成 |
| `last_error` | 截断、脱敏的诊断信息 |

### 工作包

1. **P4-1 Job/Outbox 基础设施**：加法迁移、claim/lease、fencing token、续租、完成、失败和回收；续租、完成和失败都必须按 `job_id + lease_owner + lease_revision` 条件更新。
2. **P4-2 Worker Runtime**：Job registry、并发上限、优雅退出和结构化日志。
3. **P4-3 舆情剖析试点**：第一个迁移 Job，先 shadow enqueue，再切换执行权。
4. **P4-4 评论 AI**：用持久化领取替代进程内无限循环；不改变模型、提示词和调用优先级。
5. **P4-5 媒体与报告**：媒体落地、报表、历史修复按风险逐项迁移。
6. **P4-6 Admission 边界**：第一阶段只运行一个 AI Worker；扩为多个 Worker 前必须实现跨进程总并发控制。
7. **P4-7 切换协议**：每类 Job 明确唯一执行权、提供商幂等键/结果查询/人工处置策略，以及“停止旧领取 → 等待或隔离在途租约 → 切换执行权 → 开启新领取”的顺序。
8. **P4-8 最小观测前置**：在任何 shadow/cutover 前提供队列长度、最老 Job 年龄、租约回收、Worker 最后成功循环和重复副作用防护指标；P5 再做全链路完善。

### Gate G4

- [ ] Worker 在领取后被强制终止，租约到期后 Job 可按既有重试预算恢复，任一时刻只有一个有效租约持有者。
- [ ] Job 被新 Worker 重领后，旧 `lease_revision` 的续租、完成和失败回写全部被拒绝。
- [ ] 幂等键防止同一业务事件重复创建有效 Job。
- [ ] 外部调用成功但落库响应丢失时，重试不会重复产生客户可见副作用。
- [ ] AI Job 迁移前后模型、路由、提示词、超时和调用条件一致。
- [ ] 原始采集写入不等待后台 Job 完成。
- [ ] dead Job 可诊断、可人工重放，但不会无限自动重试。
- [ ] shadow 阶段证明新旧候选集合一致后才切换执行权。
- [ ] 每类外部副作用都有可验证的幂等机制或明确的人工不自动重试处置。
- [ ] 切换前已能观察队列、积压年龄、Worker 循环、租约回收和重复防护。

### 灰度与回滚

- 每种 Job 有独立开关；只切换未来任务，不删除历史。
- 先 shadow enqueue，不执行外部调用；比对一致后，按唯一执行权协议切换。
- 回滚顺序固定为：停止新 Worker 领取，等待可安全完成的在途任务或使其租约失效并隔离，确认无新路径有效持有者，再恢复旧调度；全程保留 Job 台账。

## P5：可观测性与发布可靠性

### 目标

能够区分“进程在线、队列在跑、业务在推进”，并使发布具备明确构建清单、健康门禁和可恢复回滚点。

### 工作包

- 统一请求、任务、Agent、Job 的上下文字段：`requestId`、`tenantId`、`taskId`、`itemId`、`agentId`、`jobId`。
- 结构化日志替代关键链路的自由文本 `console.*`；所有错误脱敏和截断。
- 暴露或汇总以下运行指标：
  - API 5xx、延迟和数据库池等待；
  - Scheduler 最后成功时间与调度延迟；
  - 队列长度、最老 Job/Work Item 年龄、租约回收数量；
  - Agent 心跳、任务心跳和业务进展分别统计；
  - AI admission active/queued/oldest wait 和成功吞吐；
  - stale revision 拒绝数、领取冲突数和重复副作用防护数。
- 发布同时构建 Admin 与 Dashboard，生成版本/提交/迁移/静态资源清单。
- 发布目录版本化；备份、迁移、切换、PM2、readiness、业务冒烟和回滚材料分别验证。
- 不把现有 `deploy/deploy.sh` 描述为原子发布，直到实现与验证完成。

### Gate G5

- [ ] 关闭数据库时 readiness 失败而 liveness 仍能正确表达进程状态。
- [ ] Scheduler 或 AI Worker 停止时，API 健康不再被误报为整套系统健康。
- [ ] 可从一个 task ID 追踪 Agent、Command、Attempt、Item、Job 和最终结果。
- [ ] 发布包能证明构建来源、迁移集合和静态资源版本。
- [ ] 测试环境完成上一版本回滚演练；数据库向后兼容边界有记录。
- [ ] 未得到部署授权前，不触碰生产 PM2、Nginx、数据库或客户 Extension。

### 灰度与回滚

- 日志与指标先在本地/测试环境启用，确认脱敏、容量和告警阈值后再单独申请环境发布。
- 发布继续使用版本化目录和加法迁移；应用回退只切回上一已验证版本，不删除新表、新列或审计数据。
- 若新健康门禁误报，先恢复上一版探针配置；不得为了显示“健康”而关闭 Worker 积压或业务推进检查。

## P6：Admin 页面注册与 API 边界

### 目标

消除桌面、移动和 Sidebar 多处维护页面、权限和标题导致的漂移，并阻止旧请求覆盖新筛选结果。

### 工作包

1. 建立单一 Page Registry，包含页面 ID、标题、工作区、权限、桌面/移动可见性、懒加载组件和 Badge 配置。
2. Desktop、Mobile 和 Sidebar 只消费 Registry；允许各端保持不同布局，不强迫视觉统一。
3. API 客户端增加运行时响应校验、稳定错误码和统一 401/session-expired 处理。
4. 建立统一查询 hook：AbortController、generation fence、loading/error/empty 状态和租户切换取消。
5. 先拆数据请求与状态，再拆 2,000 行级页面组件；不顺带重做视觉。

### Gate G6

- [ ] 同一页面只在 Registry 定义一次身份、权限和加载器。
- [ ] 桌面、移动和 Sidebar 的可见页面、平台管理员权限测试一致。
- [ ] 租户或筛选快速切换时，旧响应不能覆盖新结果。
- [ ] API 返回结构漂移在边界被检测，而不是在组件深处报错。
- [ ] 关键运营列表保持空结果时表头和筛选可用，滚动结构不回退。
- [ ] 本阶段修改文件 lint 通过，Admin 全局 lint 债继续下降。

### 灰度与回滚

- Page Registry 和查询层先通过兼容适配器供一个低风险页面使用，再逐页迁移；不一次删除旧映射。
- 出现权限、路由、移动端可见性或请求时序差异时，恢复该页面旧注册/请求路径；其他已验证页面不连带回滚。
- 本阶段默认无数据库迁移；如后续确需加法字段，必须拆成独立工作包和 Gate 证据。

## P7：Extension 渐进模块化

### 目标

降低超大文件修改风险，同时保持 Manifest、消息协议、本地账本、Debug 所有权和平台采集行为兼容。

### 拆分顺序

1. **消息协议与 DTO**：集中消息名、请求/响应和版本兼容。
2. **任务状态机**：锁、Attempt 栅栏、检查点、恢复和终止吸收。
3. **平台适配器**：小红书、抖音、微博分别收敛页面识别、列表、详情、评论和作者能力。
4. **同步与离线队列**：记录 envelope、批量同步、取消和幂等。
5. **Sidebar 控制器与视图**：状态派生与 DOM 渲染分离。

### 实施方式

- 原入口保留 facade，逐函数迁移并保持导出兼容。
- 每次只拆一个稳定协议或一个状态机，不同时改变页面选择器。
- `extension-build/` 始终由同步脚本生成，不直接编辑。
- 每个平台拆分都需要目标真实页面测试；DOM 契约断言不能作为唯一证据。

### Gate G7

- [ ] 现有 Extension 回归测试全部通过。
- [ ] 源码与本地 `extension-build/` 交付快照一致；客户已安装版本另行授权核验。
- [ ] 手工、云端一次性、无人值守、停止和恢复链路均通过真实浏览器验收。
- [ ] Agent、租户、任务和 Attempt 切换不会复活旧状态。
- [ ] 抖音/小红书平台安全验证仍是人工介入，不因拆分增加重试或并发。
- [ ] 客户浏览器仍需明确升级和 Reload；服务端发布不会被描述为浏览器已升级。

### 灰度与回滚

- 每次仅切换一个协议、状态机或平台适配器，并保留原 facade 一个预先定义的完整观察周期。
- 出现消息协议、锁、Attempt、检查点或真实页面行为差异时，恢复对应 facade 调用，不同时回滚其他已验证模块。
- 发布前保存最后兼容 Extension 包、版本号和受影响浏览器/Profile 清单；回滚仍需人工安装并 Reload，不能假设服务端回退会自动回退浏览器。

## P8：兼容层和遗留入口收尾

### 目标

在新边界稳定后删除临时 re-export、`PROCESS_ROLE=all` 兼容依赖和确认无调用的 Legacy 页面，降低永久双轨成本。

### 删除门槛

- 至少一个预先定义的完整观察周期没有旧路径调用；该周期需覆盖所有受影响调度至少一次，且不短于 7 个自然日；
- 仓库搜索、运行日志、API 清单和真实浏览器均无使用证据；
- 回滚版本不再依赖被删除接口；
- 用户文档、API 文档、部署手册和故障手册已经同步；
- 删除作为独立 PR，不与新功能混合。

### Gate G8

- [ ] 最后一个兼容版本、构建来源和恢复包已归档并验证。
- [ ] 旧入口在一个完整、预先定义的观察周期内调用次数为 0。
- [ ] 删除后的 API、进程角色、Admin 与 Extension 冒烟通过。
- [ ] 已在测试环境演练恢复被删除入口或回退到最后兼容版本。
- [ ] 回滚窗口、负责人、证据日期和关闭条件已记录。

### 回滚

- 删除仅以独立提交/PR 进行；回滚优先恢复该提交和最后兼容配置，不执行数据反向迁移。
- 若新代码已写入旧版本未知但可忽略的加法字段，旧版本必须已被兼容性测试证明可运行；否则禁止删除。

## 7. 验证矩阵

| 层级 | 最低要求 |
|---|---|
| 纯函数 | 状态转换、恢复处置、聚合、输入规范化 |
| Repository | 真实 PostgreSQL、约束、事务、行锁、迁移 |
| 应用用例 | 并发领取、命令过期、离线回收、幂等和旧写入栅栏 |
| HTTP | 真实 Express app、认证、租户、状态码、错误契约、健康端点 |
| 多进程 | 两个 Worker/Agent 并发领取、崩溃、租约过期、重新领取 |
| Admin | 构建、lint、权限、请求取消、关键桌面与移动路径 |
| Extension | Node 回归、源码/交付一致性、Chrome/Edge 真实页面 |
| 发布 | 干净构建、备份、迁移、readiness、业务冒烟、回滚演练 |

不能以单一测试层通过代替其他层。例如，全量 Node 测试通过不能替代 PostgreSQL 竞态测试；PM2 online 不能替代 AI 队列吞吐检查。

## 8. 指标与推进门槛

### 8.1 正确性硬指标

| 指标 | 要求 |
|---|---|
| 同一 Work Item 同时存在两个有效执行者 | 0 |
| 旧 revision / 旧 execution task 写回被接受 | 0 |
| 因 Worker 重启永久丢失持久化 Job | 0 |
| 跨租户读取、领取或回写 | 0 |
| AI 失败导致原始采集回滚 | 0 |
| 未授权的生产变更 | 0 |

### 8.2 工程指标

- CI 必需检查通过率和平均耗时；
- Admin lint 存量从 288 单调下降，不允许增加；
- Dashboard build/lint 恢复并保持通过；
- 真实数据库/API/多进程测试占比逐阶段增加；
- 超大核心文件行数下降只能作为结果指标，不能以移动行数代替职责解耦。

### 8.3 运行指标

正式设定绝对 SLO 前，先采集至少一个完整业务周期的当前基线。灰度阶段要求：

- 每个 Gate 在开始前补齐责任人、日期、验证环境、复现命令/证据链接和量化阈值；默认只使用本地或隔离测试环境；
- 生产查询、生产快照或生产只读核验均不属于默认验证手段，必须单独授权；
- API 5xx 和 p95 延迟的容许变化在灰度前由基线数据确定，不使用“明显回退”作为验收文字；
- heartbeat 事务 p95 的容许变化在灰度前量化并记录；
- 最老排队 Job/Work Item 年龄不持续增长；
- Scheduler 实际执行时间与计划时间偏差可解释；
- 租约回收、领取冲突和 stale-write 拒绝均可观察；
- AI 模型、调用条件和费用不因架构迁移增加。

## 9. 分支、提交与发布策略

### 9.1 分支

- 实施分支使用 `codex/` 前缀。
- G0 前不开始跨文件结构改造。
- 当前功能分支若先合并，则从合并后的 `main` 创建架构分支；若不合并，必须在架构分支说明对 `1332176` 的依赖。

### 9.2 提交

- 一个提交只做机械移动、测试、行为改变或文档中的一种主要事项。
- 每阶段完成后先本地验证；“提交”不自动授权推送。
- 推送不自动授权合并；合并不自动授权生产部署。

### 9.3 发布

- 数据库、Server、Admin、Dashboard、Extension 分别列出发布范围。
- 数据库备份、应用备份、构建、迁移、进程重启、健康、业务验证和回滚材料分别报告。
- Extension 发布必须说明哪些浏览器/Profile 尚需升级和 Reload。

## 10. 当前执行看板

| 项目 | 状态 | 证据/下一步 |
|---|---|---|
| 架构只读审查 | 已完成 | 当前分支、核心链路、测试、构建、迁移和发布均已抽查 |
| P0-1 基线证据 | 已完成 | 1133 测试、Admin build、lint 和 Dashboard 故障已记录 |
| P0-2 分支决策 | 已完成 | 本地实施分支明确依赖 `1332176` 的 5 个未合并提交；不宣称已进入 `main` |
| P0-3 架构决策 | 已完成 | 本文固定模块化单体与独立 Worker 方向 |
| P1-A 工程基线 | 已本地提交 | Dashboard build/lint、Admin lint 基线、Node 版本、服务端 test 入口均已本地验证 |
| P1-B CI 与测试骨架 | 已完成 | Bash 修复已推送；远端 run `31611209688` 的质量与 PostgreSQL 14/16 任务全绿 |
| Gate G1 | 已关闭 | 干净 checkout 远端证据为 run `31611209688`；未据此推导合并或部署 |
| P2-A1 App 工厂 | 已合并并限定发布 | 分支末端 `bfc43fc`；PR #19 最终 CI `31791498224` 全绿；主线合并 `0ec5508`；生产为 `236aa4d` + 4 文件白名单 |
| P2-A2 | 已合并 | PR #20 最终 head `c0826e0` 的 push/PR 两组共 10 个任务全绿；合并提交 `5b66004`；不需要生产发布 |
| P2-B | 已合并、未部署 | PR #21 于 2026-08-15 合并为 `7c3e47ef1717a3fc498d63a1e3b7952e9100ecee`；生产仍是“`236aa4d` + P2-A1 四文件”，没有配置 `PROCESS_ROLE` 或角色锁的已部署证据 |
| P2-C | Draft PR 候选 | 从 `main@7c3e47e` 创建 `codex/architecture-modernization-p2c-process-runtime`；实现提交 `bc4d665` 已推送并创建 Draft PR #22；独立入口、健康语义、统一后台 drain、非 reset 迁移全集校验与 split candidate 已通过两版 Node/PG 及工程门禁，但尚未合并、未部署，split 仍被历史 one-shot、P2-D/P2-E 和发布授权阻断 |
| P2-D–P8 | 待开始 | 下一生产比对基线必须使用“`236aa4d` + P2-A1 四文件”精确组成，不得仅依赖 `RELEASE_COMMIT` 标记；后续阶段仍需单独授权 |

## 11. 第一实施批建议范围

P1 拆为两个候选批次；批次 A、B 与 CI 兼容修复均已按白名单提交、推送并通过统一 G1。P2-A1 是新的独立本地批次，不改变 P1 的远端提交历史。

### 批次 A：恢复构建与固定基线

- 修复 Dashboard TypeScript 构建；
- 清理 Dashboard 16 个 lint 错误；
- 增加服务端标准 test 命令；
- 记录 Admin lint 288 基线并建立“不增加”检查；
- 记录当时实施分支为 `0.3.80/063`，但在功能提交合并和发布前不强行把稳定/生产交接文档改成未发布基线。

### 批次 B：CI 与真实测试骨架

- 新增 CI 工作流；
- 在本地全新临时副本运行全量测试和双前端构建，并由获批推送后的远端 CI 补齐干净 checkout 证据；
- 增加仓库源码到本地 `extension-build/` 交付快照的一致性检查，不访问生产或客户浏览器；
- 建立后续 `createApp()` 和 PostgreSQL 集成测试的目录、运行约定；
- 不在此批次抽离 `server/index.js` 或修改生产启动方式。

批次 A、B 已完成并统一通过 G1；P2-A1 已完成提交、推送、PR #19 合并和 4 文件限定生产发布；P2-A2 纯测试批次已通过最终远端门禁并由 PR #20 合并为 `5b66004`，不需要生产发布。P2-B 已通过 PR #21 合并为 `7c3e47e`，但生产部署未获授权、未执行。P2-C 已获授权在该新主线基点本地实施；当前本地候选不自动获得提交、推送、PR、合并、P2-D/P2-E 或生产部署权限。

## 12. 阶段完成报告模板

每阶段结束统一报告：

1. **范围**：本阶段明确改了什么、没改什么；
2. **本地状态**：分支、工作区、变更文件清单；
3. **验证**：测试、构建、lint、数据库和浏览器证据；
4. **行为对比**：外部协议、状态、不变量和性能是否变化；
5. **数据边界**：迁移、备份、影响行数和未触碰租户；
6. **发布边界**：是否提交、推送、合并、部署；
7. **回滚点**：代码、数据库、配置和 Extension 的恢复方式；
8. **下一 Gate**：尚未满足的条件和下一小步。
