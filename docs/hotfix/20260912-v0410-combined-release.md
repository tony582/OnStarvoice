# v0.4.10 合并发布说明

本次将客户群助手和“排查采集日期混入问题”任务中的抖音明确空结果修复一起发布。用户已授权统一提交、推送和部署。发布基线为已核实的生产 v0.4.9，提交 `a38a7f5a8983636b9ab37525b75c9c4cbd26e0d2`；抖音修复来源为 `02e9210e7c691a974a51aa86ae8d649234070b1b`。

## 发布内容

- 抖音“搜索结果为空”在当前关键词和筛选已确认后按 0 条成功结束，继续下一关键词；不把帖子中引用的相同文字当成空结果。
- 后台新增客户助手管理与试用入口，复用 DeepSeek、已有日报和 SMTP，支持负面查询、取日报、请求日报邮件及连续追问。默认关闭，管理员聊天试用始终不外发。转述、总结、能力询问不视为发信授权。
- 新增 `084_customer_group_assistant.sql` 的四张表。迁移仅新增结构，不更改已有业务记录；应用回滚时保留这些表，不自动恢复或覆盖发布后的业务数据。
- 更新 Extension、更新提示、更新日志和控制面版本基线为 0.4.10。扩展包为 `StarVoice-extension-v0.4.10-20260912.zip`。

Server 只覆盖最终提交相对基线的 Server 文件；Admin 发布本次完整构建。保留生产环境、依赖和 Dashboard。发布清单记录每个交付文件及基线哈希、备份、迁移和实际运行证据，不能仅用 Git SHA 代替线上文件组成。

## 已完成验证

- Node 24.12.0 与生产同版 Node 18.20.8 各 2,209 项回归通过。
- 完整 PostgreSQL 集成 182 项通过，助手从空库迁移及 13 项链路测试通过。
- Admin 构建通过；助手页面、预览、成员权限和窄屏交互已验证。真实 DeepSeek、飞书及邮件端到端验收尚未完成。
- 发布使用最终提交的 CI 五项结果、ZIP 校验、备份和 staging 证据作为切换前置条件。

## 发布与验收边界

受控流程保留完整旧 Server、Admin 目录及数据库备份，先核对生产基线和进行中的采集、命令及投递，再停止既有单实例，事务应用唯一获批迁移，原子交换目录并恢复服务。健康、实际文件、迁移、更新接口、下载包、既有设备心跳均需验证；失败回切 v0.4.9 应用，保留新增表和业务数据。

客户助手部署后仍默认关闭，接入真实客户群另做应用与成员配置。上线验收不发测试邮件或群消息，不触发采集。各设备须安装新版 Extension 并重新加载，服务端和下载包更新不等于设备修复生效。本机浏览器所加载的扩展目录须先备份，再从正式 ZIP 更新；不能从旧架构源码重建。

实际发布时间、最终提交、CI、备份哈希、旧目录位置和设备版本，以 `RELEASE_MANIFEST.json` 及本次发布验收记录为准。
