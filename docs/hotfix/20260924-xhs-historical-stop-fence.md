# 小红书历史停止保护误拦修复

修复提交为 `976a0c627fd24d56618400eaced8f7a34b0a0a97`，基线为 `3043c9c82252901a9d1c643a6def409e0c140cdd`。2026-09-24 16:05（Asia/Shanghai）已部署 `server/services/capture-cloud.js` 和 `server/routes/capture-cloud.js`；无数据库迁移或 Extension 更新。

## 故障和证据

2026-09-24 早上，新加的 `PREVIOUS_CAPTURE_STOP_UNCONFIRMED` 占用判断将历史 `superseded` 任务也视为节点仍未停稳。它只识别取消、成功完成或 `recoveryTaskId`，遗漏了历史自动接力及原节点后续执行已经结束的证据。

备份中共有 49 条记录，涉及 8 个小红书节点；旧记录更新时间从 9 月 2 日至 23 日。07:14:22 的 `historical_stop_fence_reconciled` 事件解除这些占用，07:14:26 即重新出现下发事件。相关服务端源文件 SHA-256 与 `3043c9c` 一致；服务器根目录和 server 目录内的旧 RELEASE_COMMIT 文件并未同步更新，因此不能以这些旧标记判定本次运行版本。

## 修复后的判定

仍保留历史错误。仅当以下证据同时成立时，旧记录不再占用节点：

- 旧任务已被替代，没有待确认的停止操作，且存在同租户、同父批次的真实接力任务。
- 原节点在相同平台上又创建并实际启动了更晚的采集，创建与启动时间均晚于旧记录最后更新。
- 后续采集已经成功结束；若后续采集是取消状态，必须有同节点、同任务已接受并完成的停止指令回执。

配置保存、打开页面、父任务汇总、其它节点或租户的完成记录不作证明。当前未确认的停止、正在执行的任务和未完成指令继续占用节点。工作项重试策略保持原样，不因历史节点放行重启旧任务。

调度领取、心跳交付、手动下发排队原因及公平调度共用此规则。证明数据按租户和节点汇总一次，避免逐条关联查询造成 JIT 编译及重复扫描开销。

## 验证

- 使用生产同版 Node 18，185 项相关回归和 65 项隔离 PostgreSQL 集成测试全部通过。
- 新增回归先在原版复现：49 条旧记录使空闲节点无法领取。修复后 8 个节点的真实心跳处理器各返回 1 条独立创建指令，原有 49 条错误记录仍完整保留，重复领取被当前执行挡住。
- 验证真实停止、缺失接力关系、跨租户/节点/平台、无实际启动、缺失完成时间、取消未回执、配置保存和来源页面打开等反例。
- 本地临时表载入最小状态投影：6,915 条任务、6,346 条指令；恢复备份中 49 条原始错误，证明截止 07:14:22，49 条历史误拦全部消除。该回放仅验证历史停止判断，不将其它导出状态称为完整的早上数据库快照。
- 含历史错误的本地回放，8 次实际领取查询耗时约 7–10 ms，无 JIT。生产查询仅作只读验证，结果另存发布证据。

## 发布与剩余问题

GitHub Actions 运行 `35972624212` 的五个任务全部通过。上线前确认生产两份文件与基线 SHA-256 一致；备份后仅替换这两份文件并重启 `onstarvoice`。新进程 PID 为 `413492`，`health`、`live`、`ready` 均通过，部署后错误日志未新增内容。8 个 0.4.14 小红书节点没有任务占用，其中 7 个心跳新鲜；霸王龙保持部署前已有的离线状态。

回滚副本位于 `/opt/onstarvoice-private/releases/xhs-stop-fence-976a0c6-20260924T080518Z/server-candidate`，部署证据位于 `/opt/onstarvoice-release-evidence/0.4.14-976a0c6-20260924/xhs-stop-fence/deployment.json`。无需清理历史记录、修改任务状态或重载 Extension。

早上提到的时间筛选校验失败是另一个问题。本修复不放宽“一天内”校验，也不宣称这些失败已经恢复。原 07:05 批次已于 09:09 被停止，本次诊断没有重新启动它。
